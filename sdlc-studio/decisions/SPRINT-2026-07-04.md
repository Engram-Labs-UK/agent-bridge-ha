# Decisions Ledger - SPRINT-2026-07-04

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-03T23:39:19Z | Sprint batch = full clearable backlog: BG0012-BG0015 + CR-0014/0015/0016 + CR-0011 (8 units). CR-0002/CR-0004 excluded: Review status, component-complete, remaining ACs operator/cross-repo-gated (IMPLEMENTATION-KICKOFF.md) - not clearable from this repo. | Operator asked to clear the full backlog; only in-repo-actionable units qualify. |
| 2026-07-03T23:39:19Z | audit.py 'underspecified' verdicts on BG0012-BG0015 ruled FALSE POSITIVE: the check requires '## Steps to Reproduce'+'## Proposed Fix' headings but the skill's own templates/core/bug.md uses '## Reproduction Steps'+'## Fix Description'. All four bugs carry repro steps and a proposed fix. Skill-level defect, applies to every bug in this repo (BG0001-BG0015). | Deterministic gate vocabulary drifted from its own template (LL0008 class); content readiness verified by inspection. Raise upstream against sdlc-studio skill. |
| 2026-07-03T23:39:19Z | audit.py unmet-deps on CR-0014 (BG0014 Open) and CR-0011 (CR-0014/15/16 Proposed) ruled EXPECTED: in-batch sequencing edges declared for wave ordering; the waves deliver the dependency before the dependent. | The plan's dependency waves exist precisely to satisfy these edges in order. |
| 2026-07-03T23:39:19Z | CR-0016 moved to wave 2 despite no hard dependency: it edits config_flow.py and services.py, which wave-1 units BG0015 (config_flow.py) and BG0013's consumers (services.py tests) also touch; sequencing avoids parallel-agent file conflicts. | Units in one wave must be genuinely independent (parallelisable); shared-file edits are not. |
| 2026-07-04T07:23:32Z | TRIAGE APPROVED (operator, 2026-07-04): full batch, all 3 waves, run to done. CR-0016 Item 1 resolved: implement the default-agent fallback in send_message/ask_with_image (not a YAML rewording). | Operator sign-off at the triage STOP; Item 1 matches the memory services' existing behaviour and the documented contract. |
| 2026-07-04T07:51:19Z | Independent critic (separate agent instance, property-probe on BG0012 parser ~4000 chunkings) APPROVED all 7 code units. Acted on obs#2: registration-collision fallback added to async_register_webhook (CR-0014's own risk mitigation) + test. Ruled by-design: obs#3 pushed status stored raw (consistent with the shallow-poll path storing raw status; tristate normalisation applies only to the /v1/health bridge field). Deferred as cosmetic: obs#1 marker-only stream duplicate bridge call (noted in BG0012), obs#4 stale persisted sub id never cleared, obs#5 ssl_verify choice lost on form redisplay, obs#6 fallback default agent not roster-validated (matches memory services). | Critic gate D3: full depth on parser/security units, light on mechanical; verdicts recorded via critic.py. |
| 2026-07-04T08:20:46Z | CLOSING GATE PASS: adopted sdlc-studio/.config.yaml (status_vocab extensions cr:[Implemented,Review] epic:[Review] — files are truth, history not rewritten; conformance.adopt_after US0039 — verified/critiqued discipline forward-only from this sprint). CR-index Dependencies table converted to prose (the index parser reads its rows as duplicate CR ids; the skill's own cr index template has the same flaw — second upstream defect alongside audit.py's bug-heading mismatch). reconcile apply fixed one pre-existing stale row (EP0008 In Progress -> Review, visible once Review became canonical). Manifest bumped 0.11.1; tag/release + live update hand back to the operator. | Gate green means green: conformance 0, validate 0, duplicate-id 0, reconcile drift 0, retro present. |
