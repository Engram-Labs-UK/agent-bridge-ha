<!--
Template: Change Request
File: sdlc-studio/change-requests/CR{NNNN}-{slug}.md
Status values: See reference-outputs.md
Related: help/cr.md, reference-cr.md
-->
# CR-0008: Re-baseline to bridge v4.137 + capability audit

> **Status:** Implemented
> **Priority:** P1
> **Type:** spec-gap
> **Requester:** Darren Benson
> **Date:** 2026-06-09
> **Affects:** const (drift baseline), client, webhook, CI
> **Depends on:** —
> **GitHub Issue:** —

## Summary

The integration is built and tested against bridge **v4.36.0** while the live bridge is **v4.137.1** - roughly 100 minor releases of new capability. Re-baseline the drift pin, audit the capability delta to decide what is HA-relevant, subscribe to the new `message:sent` webhook event, and validate the client against the bridge's now-published OpenAPI 3.1.1 spec. This is the hygiene gate that should precede any new feature work (CR-0009, CR-0010).

## Problem

`const.TESTED_BRIDGE_VERSION = "4.36.0"`. The live bridge reports 4.137.1 (`agent-bridge/package.json`). The US0029 drift check raises an HA repair issue whenever the live bridge moves past the baseline, so the operator is almost certainly seeing a standing repair notice. Beyond the noise, ~100 releases of new capability (usage/cost, doctor, memory, governed skills, OpenAPI spec, honest pagination, async messages) have landed unexamined, and at least one webhook event the bridge now emits - `message:sent` - is not in `BRIDGE_WEBHOOK_EVENTS`.

---

## Proposed Changes

### Item 1: Bump the drift baseline

**Priority:** P1
**Effort:** S

Set `const.TESTED_BRIDGE_VERSION` to the validated live version (4.137.x). Confirm the existing chat/discovery/health calls still behave against it (live smoke). Note: this is the *bridge* pin and is independent of the HA-core pin in Critical Rule 9.

### Item 2: Capability-delta audit (4.37 → 4.137)

**Priority:** P1
**Effort:** M

Walk the bridge `agent-context` `notable_capabilities` delta. For each, record HA-relevant / out-of-scope with a one-line reason. The HA-relevant set feeds CR-0009 (usage/cost, doctor) and CR-0010 (memory); the out-of-scope set (provider keys, HA failover, crew-trust, config mgmt) is documented so it is not revisited. Output: a short audit note in this CR's close reason or a `reviews/` snapshot.

### Item 3: Subscribe to `message:sent`

**Priority:** P2
**Effort:** S

Add `message:sent` to `BRIDGE_WEBHOOK_EVENTS` and handle (or deliberately ignore) it in `webhook.py`. Confirm the full v4.137 event catalogue against the bridge: `agent:registered`, `agent:unregistered`, `agent:updated`, `agent:health-changed`, `message:sent`, `message:error`, `bridge:upgraded`.

### Item 4: Validate the client against the OpenAPI spec

**Priority:** P2
**Effort:** M

The bridge now publishes OpenAPI 3.1.1 at `GET /v1/openapi.json` (CI-guarded against doc drift). Add a test that checks the `BridgeClient` request/response shapes for the endpoints HA consumes against that spec, so future bridge contract changes surface in CI rather than at runtime.

---

## Impact Assessment

### Existing Functionality

No behavioural change intended - this validates that current calls still hold against v4.137 and silences the drift repair issue. Any contract break found is fixed as part of the audit.

### Affected Modules

| Module | Impact | Change Type |
| --- | --- | --- |
| `const.py` | Bridge baseline + webhook event list | Modified |
| `webhook.py` | Handle `message:sent` | Modified |
| `client.py` | Possible shape fixes from the audit | Modified |
| `tests/` + CI | OpenAPI-shape validation | Added |

### Breaking Changes

None expected; the audit exists to catch any.

---

## Acceptance Criteria

- [x] `const.TESTED_BRIDGE_VERSION` matches the validated live bridge (4.141.0); version-drift arm clears
- [x] Capability-delta audit recorded (HA-relevant vs out-of-scope) -- see Close Reason
- [x] `BRIDGE_WEBHOOK_EVENTS` includes `message:sent`; webhook handler accounts for it (deliberate no-op + debug log)
- [x] Client shapes validated against `/v1/openapi.json` for consumed endpoints, in CI (`tests/test_openapi_contract.py` + vendored fixture)
- [x] Live smoke: `/health` + `/v1/openapi.json` confirm v4.141.0; all 12 consumed endpoints present in the spec
- [x] `ruff check` + `ruff format --check` green; pytest green in CI

---

## Risks

| Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| A consumed endpoint changed shape across 100 releases | Med | High | OpenAPI validation + live smoke before merge |
| Audit scope balloons | Med | Low | Time-box; only classify, don't build (features are separate CRs) |

---

## Dependencies

### CR Dependencies

None. This gates CR-0009 and CR-0010.

### External Dependencies

| Dependency | Type | Status |
| --- | --- | --- |
| Live bridge v4.137 reachable for smoke | infra | Available (10.0.0.210) |

---

## Linked Epics

> *Populated when CR is actioned via `/sdlc-studio cr action`*

| Epic | Title | Status |
| --- | --- | --- |
| — | — | — |

---

## Out of Scope

- Building any new feature surfaced by the audit (CR-0009, CR-0010, and future CRs).
- Async long-running tasks (`POST /v1/messages` + callback) - parked as a future candidate; overlaps the US0037 webhook ingress.

---

## Open Questions

- [ ] Pin to an exact 4.137.x or to a floor? - Owner: Darren Benson

---

## Close Reason

**Outcome:** Complete
**Rationale:** Implemented in Wave 3 (`chore/cr0008-rebaseline`). Live smoke showed the running bridge at **v4.141.0** (the disk repo was 4.137.1; the running instance had moved on again), so `TESTED_BRIDGE_VERSION` is pinned to `4.141.0`. Added `message:sent` to `BRIDGE_WEBHOOK_EVENTS` and a deliberate no-op handler in `webhook.py`. Added `tests/test_openapi_contract.py` + a vendored slice of the live `/v1/openapi.json` (`tests/fixtures/bridge_openapi_contract.json`) that asserts every consumed endpoint is present and the chat request schema carries `messages` -- future bridge drift now fails in CI, not at runtime. Bumped the stale `3.2.0` canned versions in `conftest.py`.

**Capability-delta audit (4.36 -> 4.141), HA-relevant:** usage/cost (`/v1/agents/{id}/usage`, typed) and fleet doctor (`/v1/doctor`) -> CR-0009; agent memory (`/v1/agents/{id}/memory`) -> CR-0010; richer `/v1/health` (operationalState 7-state, `toolSurface.{advertised,wired,missing}`, `circuit`) -> future enrichment candidate; honest pagination (`hasMore`) on inbox/memory/skills.
**Out of scope (operator/infra, thin-adapter):** provider keys/slots, HA failover promote/demote, crew-trust assertions/appointments/revocations, config management, admin token rotation, maintenance skills, tool-permission policy, traces/runs, workspace files, the agent email stack, scheduled reports, operator prefs/topics.
**Parked:** async long-running messages (`POST /v1/messages` + callback) -- overlaps the US0037 webhook ingress.
**Finding (no action):** the client sends `caller_context` + `attachments` on `/v1/chat/completions`; the bridge accepts them (US0034 works live) but the OpenAPI does not document those fields. Flagged for a future bridge-side spec fix; not an HA gap.

Live confirmation that the HA drift repair issue clears is pending a reload of the live install (version arm clears on the pin; component-complete otherwise).

---

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-06-09 | Darren Benson | CR proposed (inverse review: bridge feature gap) |
