@AGENTS.md

<!-- Claude Code reads this file. The canonical instructions live in AGENTS.md
     (also read by Codex, Copilot and opencode). Current state is
     sdlc-studio/reviews/LATEST.md; AGENTS.md tells you to read it (a link, not an
     import, so a large review snapshot never loads into every session). -->
